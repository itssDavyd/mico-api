services:
  backend:
    networks:
      - default
      - thor_shared

  worker:
    networks:
      - default
      - thor_shared

networks:
  thor_shared:
    external: true
    name: ${THOR_NETWORK:-thor_rag_net}
